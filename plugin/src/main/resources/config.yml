# The default language for the console and unknown languages
Language: en_us

# When true, overrides the default maximum stack sizes of all items with the Max Stack Override amount
Override Default Stack Sizes: false

# The default maximum stack size for all items if Override Default Stack Sizes is true
Max Stack Override: 64

# Allows wearing stacked armor in an armor slot
Stacked Armor Wearable: false